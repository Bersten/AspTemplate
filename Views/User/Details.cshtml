@model Exercice.Models.User

<h1>Détails de l'Utilisateur</h1>

<table>
    <thead>
        <tr>
            <th>Champ</th>
            <th>Valeur</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><strong>Nom :</strong></td>
            <td>@Model.Name</td>
        </tr>
        <tr>
            <td><strong>Biographie :</strong></td>
            <td>
                @Html.DisplayFor(m => m.UserProfile)
            </td>
        </tr>
    </tbody>
</table>

<a href="@Url.Action("Index", "User")" class="btn btn-primary">Retour</a>